<div class="container">
    <div class="row">
        <% page.posts.each(function(post,index) { %>
            <%- partial('box/box_article', { post: post, index: true }) %>
        <% }); %>
    </div>
</div>

<%- partial('widget/article_scroll_button') %>

<% if (page.total > 1) { %>
    <nav id="page-nav">
        <%- paginator({
            prev_text: '&laquo; ' + __('nav.prev'),
            next_text: __('nav.next') + ' &raquo;'
        }) %>
    </nav>
<% } %>
